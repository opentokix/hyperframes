# Section 09 — Counters and Data

Scenes that visualize numbers, timelines, and pipelines. The counter pattern (deterministic `tl.set(textContent)` at evenly-spaced timestamps) is mandatory for any counter animation in a HyperFrames composition — `onUpdate` callbacks do not fire reliably during snapshot/render seek.

**When to study this section:** any beat with stats, growth claims, pipeline/flow diagrams, educational explainers, or timelines.

---

## Scenes

| Scene | Duration | Technique | Why study |
|-------|----------|-----------|-----------|
| [`scene-01-timeline-diagram/`](scene-01-timeline-diagram/) | 8.5s | Editorial cream/parchment background. Build phases: eyebrow "anatomy of a composition" → div rectangle + timeline axis with tick marks → second div docks beside the first → div changes to styled state → SVG easing curve draws + teal slider dot follows it via MotionPath → camera zooms onto curve → exit blur. | Cinematic educational viz. Demonstrates how to BUILD a diagram on screen, not just present it. SVG path drawing + MotionPath + camera zoom + multi-phase staging. |
| [`scene-02-pipeline-diagram/`](scene-02-pipeline-diagram/) | 10.7s | Vertical scroll-pan camera through a 4-step pipeline: Agent (writes HTML) → Renderer (captures every frame) → MP4 output → Deterministic proof (identical card stacks split apart with green check badge). SVG dashed flow connectors with orange pip markers slide between stages. | Demonstrates pipeline/flow-diagram beat: scroll-pan motion + SVG connector draw + icon choreography + payoff with side-by-side cards proving determinism. |
| [`scene-03-counter-million-showcase/`](scene-03-counter-million-showcase/) | 6s | Canonical 0 → 1,000,000 counter with comma formatting. 33 deterministic `tl.set(el, {textContent: "..."}, t)` calls spaced along the timeline, plus stat-card glow + value-color flash at each milestone (10K / 100K / 500K / 1M). | **The reference pattern for any counter in any composition.** Discrete `tl.set()` at fixed timestamps is the ONLY reliable counter pattern under HyperFrames' `tl.seek()` — `onUpdate` callbacks do not fire when the engine scrubs to a specific time. Copy this pattern for any 0→N counter regardless of magnitude (users, dollars, percent, etc.). |
| [`scene-04-bar-chart-rise/`](scene-04-bar-chart-rise/) | 7s | **6-bar quarterly-growth chart.** Y-axis with 5 tick marks (0/250/500/750/1000), 6 bars (Q1 → Q2·26) staggered rise with `back.out(1.2)` overshoot. Each bar has a deterministic value counter above (89 / 162 / 248 / 421 / 612 / 847) that ticks up in ~10 steps as the bar rises. Peak bar (Q2·26) renders in amber instead of blue to draw the eye. Headline reads "Videos shipped 847% YoY". | The reference for **bar chart rises** with deterministic counter labels. Pattern: each bar's height animated via `tl.fromTo(bar, {height: "0%"}, {height: "XX%"})`, value counter via `tl.set()` at evenly-spaced timestamps. The single amber bar against the blue palette is the "where the story lands" detail. |
| [`scene-05-sparkline-draw/`](scene-05-sparkline-draw/) | 7s | **4-card live-metrics dashboard with SVG sparklines.** Active users (142k, blue climbing curve), Revenue $87K MRR (green smooth climb), API calls 2.4M/day (amber jagged peaks), Avg latency 84ms (purple declining curve — "down" delta because lower is better). Each sparkline has a filled-area path BELOW + a stroked line on TOP, both animated via `stroke-dashoffset`. End-of-line dot fades in + pulses on each card. Value counters tick up in sync with the sparkline draw. Delta arrows (+24.3% / +18.7% / +9.4% / −32%) fade in last. | The canonical multi-stat dashboard pattern. **Path-draw via stroke-dashoffset is the load-bearing technique** — fully seekable, no `onUpdate`. Use for any beat showing live product analytics, KPI dashboards, or growth metrics. The 4 distinct curve shapes (steady climb / smooth climb / volatile / decline-is-good) cover the common metric shapes. |
| [`scene-06-donut-chart-reveal/`](scene-06-donut-chart-reveal/) | 7s | **4-segment SVG donut chart with sequential reveal.** Video mix breakdown (Product launch 42% blue, Social ads 28% green, Demo/explainer 18% amber, Brand reels 12% pink). Each segment is a `<circle>` with `stroke-dasharray` defining its arc length and `stroke-dashoffset` animating from invisible to visible. Center percentage counter ticks 0→100% over the reveal. Legend cards on the right cascade in synced with each segment's draw. Final state: full donut + 4 legend cards + center value held at 100%. | The reference pattern for **proportional data visualization**. Each segment's position is computed via dashoffset math (dasharray + offset = arc placement). All deterministic — segments fade in via opacity then draw via dashoffset (fully seekable). Use for any beat showing percentages, splits, or category breakdowns where the slices need to add up to a whole. |

---

## QC log

- scene-01: **PASS** — 6 frames; lifted from `launch-video/compositions/anatomy.html` (488 lines). Frame 5 captures the curve+slider zoom moment perfectly. Frame 6 shows clean exit blur. All technique labels preserved (`anatomy of a composition`, `<div>`, `start: 0`, `duration: 5`, `power2.out`).
- scene-02: **PASS** — 6 frames; lifted from `launch-video/compositions/engine.html` (409 lines). Frames 2-5 progress vertically through the Agent → flow connector → Deterministic block stages. Card-split at frame 5 with "Identical output, every time." badge visible. Source used generic Agent/Renderer/MP4 labels so no rebranding needed.
- scene-03: **PASS** — fully seekable counter; lint clean (0 errors, 0 warnings). 33 discrete `tl.set()` calls along the 6s duration. Frame samples at 0.5s / 1.5s / 3s / 4.5s / 5.8s each show a distinct number reading (0 → ~150K → ~600K → ~950K → 1,000,000) confirming the counter is genuinely incrementing under `tl.seek()`, not just animating on play.
